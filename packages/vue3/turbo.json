{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        "src/**/*.js",
        "src/**/*.vue"
      ],
      "outputs": [
        "dist/**"
      ]
    }
  }
}